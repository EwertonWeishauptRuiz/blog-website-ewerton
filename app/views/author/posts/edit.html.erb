<!-- Provide the page title -->
<% provide(:author, "Edit #{@post.title}") %>
<!-- put the word active where we yield blog_active -->
<% provide(:author, 'active') %>

<h1>Editing Post</h1>

<%= render 'form', post: @post %>

<%= link_to 'Show', @post %> |
<%= link_to 'Back', posts_path %>
