<!-- Provide the page title -->
<% provide(:page_title, 'New Post') %>
<!-- put the word active where we yield blog_active -->
<% provide(:author, 'active') %>

<h1>New Post</h1>

<%= render 'form', post: @post, url: authors_posts_url %>

<%= link_to 'Back', posts_path %>
